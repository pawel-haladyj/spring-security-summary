server:
  port: 8100

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/spring_security_summary?useUnicode=true&useJDBCCompliantTimezoneShift=true&useLegacyDatetimeCode=false&serverTimezone=UTC

  security:
    user:
      name: user
      password: password

application:
  name: spring-security-summary
